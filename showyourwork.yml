figure_dependencies:
  my_figure.py:
    - simulation_results.dat:
        generate: "python run_simulation.py"
        upload:
          sandbox: false
          token_name: ZENODO_TOKEN
          title: "Simulation results"
          description: "The result of a very expensive simulation."